/** DESIGN.md §2 — print-config.ts: per-client registration snippet generator (output only, no file writes) (P0 stub). */
export const PRINT_CONFIG_STUB = "print-config: not implemented (P0 scaffold)";
