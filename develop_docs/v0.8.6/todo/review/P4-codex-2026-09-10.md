# P4 외부리뷰 — codex-cli 0.153.4 (2026-09-10)

명령: `codex exec -s read-only "$(cat P4-prompt.txt)"` — 리뷰어가 파일을 수정하지 않았음을 `git status` 로 확인.
(**실측**: `--approve-for-me` 와 `-s read-only` 는 배타적이다 — "the argument '--approve-for-me' cannot be used with '--sandbox'". P3 에서 기록한 '비대화형은 --approve-for-me 필요' 는 MCP 도구 호출 시 얘기이고, 읽기 전용 리뷰는 `-s read-only` 단독이 맞다.)

[BLOCKER]

1. `cache.ts:44-71, 117-144` / `(path, mtimeMs, size)`만으로 낡은 본문을 판별할 수 없음  
   문제: 파일 내용을 같은 크기로 바꾸고 mtime을 과거 값으로 복원하면 `FRESH_WINDOW_MS` 밖에서 기존 캐시가 적중한다. `cp -p`, `rsync --times`, Git checkout, 다른 프로그램의 mtime 복원이 모두 해당한다. 시계 보정도 `Date.now()`와 파일 mtime 비교를 신뢰할 수 없게 한다. 디렉터리/볼트 realpath 변경도 키에 포함되지 않는다.  
   근거: `snapshot()`은 `fs.stat()`의 mtime·size만 키에 넣고(`cache.ts:53-69`), `getGraph()`는 키가 같으면 그래프를 그대로 반환한다(`cache.ts:117-121`). 테스트는 최근 수정 파일만 다룬다(`p4-cache.test.ts:87-100`).  
   제안: 캐시 적중 전 안전한 fd에서 내용 hash 또는 inode/dev 포함 검증을 수행하라. 최소한 root realpath·파일 realpath·inode를 키에 포함하고, mtime 역행/비정상 timestamp는 저장하지 않아야 한다.

2. `vault.ts:221-235`, `cache.ts:36-38, 53-54, 141-144` / 캐시 적중 시 보안 검사가 완결되지 않음  
   문제: `walkMd()`의 realpath 검사 뒤 `snapshot()`의 `fs.stat()`은 심볼릭 링크를 따라간다. 이후 `getGraph()`가 적중하면 `read()`가 호출되지 않으므로 `O_NOFOLLOW`가 적용되지 않는다. 파일 또는 상위 디렉터리가 검사 후 교체되면 캐시가 원래 그래프를 반환하거나, 상위 디렉터리 TOCTOU를 통해 외부 내용이 캐시에 들어갈 수 있다.  
   근거: `read()`의 `O_NOFOLLOW`는 `vault.ts:163-175`에 있지만 적중 경로는 `graph.ts:35-38`에서 read 자체를 건너뛴다. P4 테스트는 링크를 먼저 만들거나 파일을 삭제 후 링크로 바꾸는 경우만 검증한다(`p4-cache.test.ts:160-197`). P2-42+ 테스트도 직접 `read(symlinkPath)`만 검증한다(`p2-review-server-vault-cli.test.ts:108-124`).  
   제안: 캐시 적중도 보안 fd/stat 검증을 거치게 하거나, 안전하게 열린 fd 기반 snapshot/read를 사용하라. 파일 교체·상위 디렉터리 교체를 실제로 검증하는 회귀 테스트를 추가하라.

[MAJOR]

3. `cache.ts:141-144` / 권한 변경이 캐시 키에 반영되지 않음  
   문제: 캐시된 파일을 이후 chmod/read-deny 상태로 바꿔도 mtime·size가 같으면 기존 텍스트를 반환한다. 즉 현재 파일 접근 가능 여부와 캐시 결과가 달라진다.  
   근거: 적중 시 `read()`와 `fh.stat()`를 모두 건너뛰며, 키에는 mode/권한이 없다. 현재 테스트에는 chmod 후 호출 케이스가 없다.  
   제안: 권한/파일 식별자를 검증하거나, 권한 변경을 신뢰해야 하는 환경에서는 캐시 적중에도 안전한 open을 수행하라.

4. `cache.ts:185-200` / 파생값 상한이 실제 메모리 상한이 아님  
   문제: 문자열만 `derivedBytes`에 계산하고 `dl` 숫자·Map·노드 구조는 세지 않는다. `derivedBytes >= budget`만 검사하므로 마지막 문자열 하나가 예산을 초과할 수 있다. `undefined`는 저장돼도 `v !== undefined` 판정 때문에 매번 재계산된다. 또한 텍스트 캐시는 별도 eviction 없이 최대 `MAX_TOTAL_BYTES`까지 보유하고, 그래프·파생 구조 오버헤드는 추가된다.  
   근거: `cache.ts:189-195`, `cache.ts:159-172`. `MAX_TOTAL_BYTES`는 판정 상한이지 캐시 메모리 상한이 아니다.  
   제안: 남은 예산과 후보 크기를 비교하고 모든 파생값의 실제 비용을 계산하라. `undefined`용 sentinel을 사용하고, 텍스트/그래프/파생값에 명시적 eviction 또는 총 메모리 상한을 둬라.

5. `p4-12-lex-index.test.ts:58-118`, `.github/workflows/ci.yml:55-59` / 정본 구조 분기의 CI 증명이 부족함  
   문제: 단위 테스트는 하나의 고정 미니 그래프에서 TS shared/recomputed 경로만 비교한다. Python 정본은 실행하지 않는다. 퍼징 200건도 고정 픽스처에 대해 production shared 경로만 실행하므로 2패스 정본과 구조적으로 대조하지 않는다.  
   근거: 테스트 주석 자체가 “골든·패리티는 실행 경로 하나만 본다”고 인정한다(`p4-12-lex-index.test.ts:4-7`). CI에는 `python tests/parity.py` 단일 실행만 있고(`ci.yml:57-59`), `LLMWIKI_CACHE=0` 별도 실행이 없다.  
   제안: CI에서 parity를 캐시 on/off 각각 실행하고, Python 2패스와 TS 1패스를 동일한 생성 그래프(중복 slug, 중복 alias, MoC cycle, 빈 terms, 비ASCII 포함)에 대해 직접 비교하라.

6. `p4-cache.test.ts` 전체 / 필수 무효화 케이스 누락  
   문제: 다음 케이스가 없다.

   - 2초 창 밖에서 같은 mtime·size로 내용 교체
   - mtime 미래값, 시계 역행/NTP 보정
   - 파일 또는 상위 디렉터리의 symlink 교체를 walk와 snapshot 사이에 수행
   - chmod/권한 변경 후 캐시 적중
   - 동일 textual root가 다른 realpath 볼트로 바뀌는 경우
   - stat 실패 후 성공 복귀
   - 동시 호출 중 두 root를 교차 조회

   근거: 현재 P4 테스트의 변경 범위는 수정·추가·삭제·최근 수정·root 전환·정적 symlink뿐이다(`p4-cache.test.ts:44-197`).  
   제안: 위 케이스를 결정론적으로 mock하거나, 작은 helper를 주입해 TOCTOU를 재현하라.

7. `test/perf/cache-bench.mjs:37-82` / 성능 주장이 측정 범위보다 넓음  
   문제: 벤치마크는 `runExpand(... rerank: 11)`만 측정한다. `search`, `pack`, 캐시 적중 후 권한/보안 검증 비용은 측정하지 않는다. warm 측정도 “정확히 2회차”가 아니라 1회 비측정 warmup 후 10회 호출의 상위 중앙값이다. `med()`는 짝수 개에서 두 중앙값 평균이 아닌 상위 중앙값을 선택한다(`cache-bench.mjs:29`).  
   근거: 측정 호출이 `cache-bench.mjs:42-65`에만 있고, baseline은 이를 “1,000p 2회차 24.0ms”로 요약한다(`P4-perf.txt:34-40`).  
   제안: “warm 반복 median”으로 문구를 정확히 바꾸고, 정확한 2회차 측정·search/pack 측정·표준 median 계산을 추가하라.

8. 검증 명령 결과와 체크리스트 근거의 재현성이 부족함  
   문제: 현재 환경에서 `npm test`는 41개 suite가 테스트 실행 전에 `EPERM: ... mkdir .../ssr`로 실패했고 0 tests가 실행됐다. `dist`도 없어 parity/fuzz를 실행하지 못했다. 따라서 `P4-cache-followup.md:43-47, 66-69`의 “561건 통과”와 on/off 패리티를 이번 감사에서 독립 확인할 수 없다.  
   제안: CI 로그 또는 재현 가능한 실행 산출물 경로를 체크리스트에 남기고, 캐시 on/off parity를 CI 필수 단계로 승격하라.

[MINOR]

9. `cache.ts:87-97` / root cache는 하나만 유지  
   문제: 서로 다른 두 볼트를 번갈아 조회하면 정확성은 유지되지만 이전 볼트 캐시를 즉시 폐기해 반복 조회 성능이 사라진다. 동시 root 호출에서는 전역 `cur` 교체가 발생한다.  
   제안: root별 bounded cache를 사용하거나, 최소한 동시 호출을 직렬화하고 이 정책을 문서화하라.

10. P4-01 및 P4-11 기록  
   P4-01은 `[x]`이지만 실제 판정은 “트리거 미관측, 사용자 지시로 착수”로 명시되어 있어 경위는 정직하다. P4-11의 `[ ] ⏸`도 publish 미완료를 정확히 반영한다. 다만 단계 DoD의 외부리뷰 미완료 상태에서는 `status: review`가 맞고 `done`으로 바꾸면 안 된다.

총평(병합 가능 여부): 병합 불가. mtime·size 키는 낡은 결과를 보장하지 못하며, 캐시 적중 경로가 `O_NOFOLLOW`와 realpath 검증을 실질적으로 우회한다. 이는 속도 문제가 아니라 정확성·보안 BLOCKER다. 캐시 적중 시 안전한 재검증 또는 내용 기반 식별을 도입하고, on/off parity와 TOCTOU·권한·mtime 복원 테스트를 CI에서 통과시킨 뒤 재리뷰해야 한다.
