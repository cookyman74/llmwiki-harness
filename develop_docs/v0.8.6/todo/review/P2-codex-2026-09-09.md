[BLOCKER] P3 진입 불가 — `P2-34` 및 테스트 DoD 미검증

- 위치: `develop_docs/v0.8.6/todo/P2-mcp-server.md:63,78-83`
- 문제: `P2-34`가 미완료인데 DoD의 A~G·보안·스키마 검증은 `[x]`다. 실제 `npm run check`는 lint/typecheck 후 Vitest가 `EPERM: mkdir .../ssr`로 27개 스위트 전부 실행 전 실패했다.
- 근거: 테스트 0건 실행, 27개 suite failed.
- 제안: CI에서 실제 녹색 확인 전까지 DoD를 완료로 표시하지 말 것. 테스트 실행 불가 환경과 코드 실패를 분리한 재현 가능한 CI 결과를 남길 것.

[BLOCKER] realpath 검사와 실제 read 사이 TOCTOU

- 위치: `src/vault.ts:181-218`, `src/read.ts:57-61`
- 문제: `realpath()`로 하위 경계를 확인한 뒤 경로 문자열을 다시 `readFile()`한다. 공격자가 그 사이 파일을 외부 파일을 가리키는 심볼릭 링크로 교체하면 검사 후 외부 내용이 읽힌다.
- 근거:

```ts
if (!(await isInside(wikiReal, p))) ...
const raw = await read(p);
```

- 제안: 경로 검증과 파일 읽기를 동일한 안전한 file descriptor 흐름으로 결합하고, 가능한 플랫폼에서 `O_NOFOLLOW` 및 안전한 디렉터리 핸들 기반 open을 사용하라. 장기 실행 서버에서는 vault 변경 감시 또는 호출 직전 재검증도 추가해야 한다.

[BLOCKER] `wiki_pack`의 `structuredContent`가 200KB 상한을 우회

- 위치: `src/server.ts:53-58`, `src/pack.ts:72-84`, `src/tools.ts:287-290`
- 문제: `text`만 `capText()`를 거친다. `structuredContent.pages[*].claims`, `summary`, `relations`는 원문 크기 제한 없이 반환된다.
- 근거: 테스트도 의도적으로 `"pages JSON 은 온전"`을 검증한다(`p2-11-23-input-normalization-limits.test.ts:185`).
- 공격 입력: 30개 slug가 각각 수십 MB의 `claim::` 줄을 가지면 텍스트는 잘려도 구조화 응답은 수백 MB까지 커질 수 있다.
- 제안: 구조화 필드에도 페이지·필드·전체 응답별 바이트 상한을 적용하고, 초과 시 항목 단위 절단과 명시적 `truncated` 상태를 반환하라.

[MAJOR] 저수준 `Server` 채택으로 output 검증을 실제로 잃음

- 위치: `src/server.ts:80-85`, `src/tools.ts:13-18`
- 문제: `outputSchema`를 tools/list에 선언만 하고 서버 런타임에서는 검증하지 않는다. SDK의 고수준 `McpServer` 경로에는 `validateToolOutput()`가 있지만 저수준 `Server`에는 연결되지 않는다.
- 근거: 저장소의 output 테스트는 자체 부분집합 검증기와 SDK Client 경로에 의존한다. 생산 서버가 잘못된 `structuredContent`를 반환해도 차단하지 않는다.
- 제안: 수기 schema를 유지한다면 AJV 등 고정된 검증기를 서버 응답 직전에 적용하고, schema/런타임 타입을 단일 소스에서 생성하라. 불가능하면 `outputSchema`를 제거해 거짓 계약을 만들지 말 것.

[MAJOR] 파일 크기·파일 수·깊이·동시 호출 제한 부재

- 위치: `src/vault.ts:171-219,255-268`, `src/graph.ts:26-30`, `src/server.ts:85`
- 문제: 파일 하나를 제한 없이 `readFile()`하고, 모든 Markdown을 배열과 그래프에 통째로 보관한다. `readAll()` 내부 동시성 32는 파일 read만 제한하며, MCP 요청 자체에는 동시성 제한이 없다.
- 공격 입력:
  - 수 GB Markdown 한 개
  - 수십만 개 파일/깊은 디렉터리
  - 동시에 `wiki_expand` 100건 호출
- 근거: DESIGN도 “파일 크기 상한은 두지 않는다”고 명시한다(`DESIGN.md:186`).
- 제안: `stat` 기반 파일 크기 제한, 전체 페이지/디렉터리/경로 깊이 제한, 요청 큐 또는 semaphore를 도입하라. 패리티를 이유로 상한을 거부한다면 MCP 경로에는 별도 안전 상한을 두어야 한다.

[MAJOR] Windows `cmd /c` 경로 메타문자 처리 취약

- 위치: `src/print-config.ts:21-25,39-41`
- 문제: Windows 출력은 `cmd /c`를 사용하지만 `&`, `|`, `^`, `%VAR%`, `!` 등에 대한 CMD escaping이 없다.
- 공격 입력:

```text
C:\wiki\safe&whoami
C:\wiki\%PATH%
```

- 근거: 테스트는 공백·한글·`$`·백틱만 확인하고 Windows CMD 메타문자는 검증하지 않는다(`p2-26-print-config-snapshots.test.ts:156-168`).
- 제안: CMD용 quoting/escaping을 별도로 구현하거나 `cmd /c`를 제거하고 실행 파일·인자를 직접 등록하는 클라이언트별 형식을 사용하라.

[MAJOR] `--name`이 셸/TOML 명령 주입 경로

- 위치: `src/print-config.ts:37-50`
- 문제: `name`은 JSON에서는 escaping되지만 Claude/ag y/Codex 셸 명령과 TOML 테이블 경로에 검증 없이 삽입된다.
- 공격 입력:

```text
--name 'x; whoami'
--name 'x] ; malicious = true'
```

- 근거:

```ts
claude mcp add --scope user ${name} -- ${fullShell}
codex mcp add ${name} -c 'mcp_servers.${name}.startup_timeout_sec=60'
```

- 제안: 서버명에 `^[A-Za-z][A-Za-z0-9_-]{0,63}$` 같은 제한을 적용하고, 각 출력 형식별 escaping을 별도로 수행하라.

[MAJOR] read-only CI guard는 우회 가능한 정규식

- 위치: `.github/workflows/ci.yml:62-71`
- 문제: grep 기반 blacklist는 모든 쓰기 경로를 보장하지 않는다.
- 예시 우회:

```ts
fs.promises["writeFile"](...)
fs.renameSync(...)
fs.rmdirSync(...)
fs.open(path, "w")
```

- 근거: 현재 정규식은 `rename\(`만 잡고 `renameSync(`, `rmdirSync(`, 쓰기 플래그를 가진 `open()`은 보장하지 않는다.
- 제안: `fs` import를 허용 목록(`readFile`, `readdir`, `stat`, `realpath`)으로 제한하는 AST 검사와 실제 임시 vault 쓰기 감시 테스트를 병행하라.

[MAJOR] stderr 및 selftest가 절대 경로를 노출

- 위치: `src/server.ts:92`, `src/cli.ts:48,54,65`, `src/vault.ts:211`
- 문제: 문서상 debug 로그는 도구명·소요 시간뿐이어야 하지만 서버 준비 로그는 root 절대 경로를 출력한다. `--selftest`도 `root: ${root}`를 출력한다. 오류에는 입력 경로와 realpath가 포함된다.
- 근거:

```ts
debugLog(opts.debug, `ready root=${opts.root}`);
return `...\\nroot: ${root}\\n...`;
```

- 제안: 기본 및 debug stderr에서 root를 제거하고, selftest에는 경로 대신 해시 또는 `root_configured: true`만 출력하라. 오류도 사용자 입력 경로를 그대로 반사하지 않게 하라.

[MAJOR] stderr “한 줄 경고”가 파일명으로 로그 주입 가능

- 위치: `src/vault.ts:211`
- 문제: 파일명에 개행·제어문자가 있으면 경고가 여러 줄로 분할되거나 로그 형식이 변조된다.
- 공격 입력: `wiki/L3-semantic/evil\n[llmwiki] fake.md`
- 제안: 로그용 경로를 JSON.stringify하거나 제어문자를 escape하고, 경고에는 basename 대신 안전한 카운터·해시를 사용하라.

[MAJOR] 다중 클라이언트 호환성은 실제 검증되지 않음

- 위치: `develop_docs/v0.8.6/todo/review/P2-tools-list.json`, `test/unit/p2-12-13-14-schema-names-descriptions.test.ts`
- 문제: 저장소는 JSON Schema 키 부분집합만 검사한다. Gemini·Codex·Cursor·Claude 계열에서 실제 등록·tools/list·tools/call을 수행한 증거가 없다.
- 근거: `integer`, `enum`, `minLength`, `maxLength`의 표준성은 확인되지만, 각 클라이언트의 사전 검증 수용 여부는 이 테스트로 증명되지 않는다. `structuredContent` 미지원 경로도 수동 주장뿐이다.
- 제안: 최소한 각 클라이언트별 실제 handshake smoke test를 추가하고, 실패 시 스키마에서 `integer`/제약 키를 제거하거나 런타임 검증 전용으로 내리라.

[MAJOR] 테스트가 핵심 위협을 검증하지 않음

- 위치: `test/unit/p2-21-symlink-leak.test.ts:31-38,46-53`, `p2-22-path-escape.test.ts:15-64`, `p2-15-27-03-cli.test.ts:42-70`
- 문제:
  - Windows에서 symlink 생성 실패 시 전체 보안 테스트가 skip된다.
  - 대소문자 무시 FS, UNC, 드라이브 경로, NUL, NFC/NFD, 긴 경로, TOCTOU를 검증하지 않는다.
  - stdout 테스트는 3개 응답만 받고 프로세스를 종료해 이후 오염을 놓칠 수 있다.
- 제안: Windows junction 테스트를 분리하고, 플랫폼별 필수/조건부 테스트를 명시하라. 경쟁적 symlink 교체 테스트와 CMD 메타문자 테스트를 추가하라.

[MINOR] 200KB가 바이트 상한이 아님

- 위치: `src/tools.ts:20,287-290`
- 문제: `text.length`는 UTF-16 코드 유닛이다. 한글·이모지가 포함되면 실제 UTF-8/JSON 바이트가 200KB를 크게 초과할 수 있다.
- 근거:

```ts
if (text.length <= TEXT_LIMIT) ...
text.slice(0, TEXT_LIMIT)
```

- 제안: UTF-8 `Buffer.byteLength()` 기준으로 절단하고, surrogate pair·UTF-8 경계를 보존하라.

[MINOR] Unicode NFC/NFD 정규화 부재

- 위치: `src/tools.ts:258-273`, `src/graph.ts:41-45`
- 문제: NFC와 NFD가 서로 다른 slug/alias로 취급된다. macOS 파일명과 클라이언트 입력 간 lookup 실패·중복 alias 충돌이 발생할 수 있다.
- 제안: 입력·파일 slug·alias에 NFC 정규화를 적용하고, Python 정본과의 차이는 명시적 compatibility layer로 관리하라.

[MINOR] `normalizeSlugs()`가 잘못된 타입을 조용히 삭제

- 위치: `src/tools.ts:276-284`
- 문제:

```ts
arr.filter((x) => typeof x === "string" ...)
```

`[123, "valid"]`는 123을 무시하고 성공한다. `[123]`만 오류다. 입력 계약과 런타임 동작이 불일치한다.
- 제안: terms와 동일하게 비문자 항목은 즉시 `ToolInputError`로 거부하라.

[MINOR] 체크리스트의 라우팅 계약이 과장됨

- 위치: `src/tools.ts:40-41`, `P2-mcp-server.md:35`
- 문제: 모든 도구가 동일한 `ROUTING_LINE`을 마지막 줄에 넣는다. `wiki_search`가 “fallback”이라는 도구별 위치는 마지막 Routing 줄에 명확히 표현되지 않는다.
- 제안: 도구별 마지막 줄을 실제 호출 위치와 일치시키고, `instructions`가 제거되어도 description만으로 동일한 라우팅이 가능한지 테스트하라.

총평(P3 진입 가능 여부): P3 진입 불가. 현재 구현은 기본적인 symlink 엔트리 차단과 입력 상한을 갖췄지만, TOCTOU, `structuredContent` 상한 우회, 무제한 파일 read/동시 요청, Windows 명령 생성 취약점이 남아 있다. 또한 P2-34가 미완료이고 실제 테스트는 실행 전 환경 오류로 검증되지 않았다.