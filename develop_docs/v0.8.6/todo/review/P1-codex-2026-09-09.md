## [BLOCKER]

1. 위치: `develop_docs/v0.8.6/todo/P1-port-retrieval.md:87,93-98`

문제: Windows 검증이 미완료인데 DoD의 “CI 3-OS 녹색”이 이미 체크되어 있다. P1-48은 `[ ]`이며 외부리뷰도 pending이다. README의 P1 게이트는 픽스처 패리티·3-OS CI·외부리뷰 완료를 모두 요구한다.

근거: P1-48 미체크와 P1-95의 “P1-48에서 확인” 주장이 동시에 존재한다.

제안: Windows CI 실제 실행 결과를 첨부하고 P1-48을 검증한 뒤 DoD를 유지하라. 그 전에는 P2 진입 불가로 판정해야 한다.

2. 위치: `tools/llmwiki-mcp/test/unit/`

문제: “대응표 21행+#17b 각각 최소 1개 단위 테스트” DoD가 뒷받침되지 않는다.

명시적 테스트가 없는 대응표 행:

- #4 `field`
- #9 `lower`
- #12~#14 `expand` 핵심 규칙
- #15 `_score_text`
- #17b 미매치 문자열
- #21 옵션 기본값

현재 테스트 파일은 `p1-29`~`p1-42b` 일부와 `golden.test.ts`뿐이다. 골든 테스트의 간접 실행은 각 행별 단위 테스트 증거가 아니다.

근거: Python 원본 `scope-expand.py:65-67,105-119,177-182,286-298`, `search.py:47-49`에 해당하는 독립 테스트가 없다.

제안: 각 대응표 행별로 Python 실행값을 고정한 명시적 테스트를 추가하라. 특히 `field`의 Unicode 공백, `lower`의 비ASCII, `expand`의 tier/refs/MoC-K, #17b 대소문자 보존을 추가해야 한다.

## [MAJOR]

3. 위치: `tests/parity.py:157-164,184-188`, `tools/llmwiki-mcp/test/unit/golden.test.ts:55-62,87-88`

문제: rerank 점수 검증이 `NaN`을 통과시킨다.

예시: Node가 `tier\tNaN\tslug\ttype`을 출력하면 `float("nan")`은 성공하고, `abs(py - nan) > 0.05`는 `False`가 되어 통과한다. 골든 테스트의 `Number("NaN")`도 동일한 허점이 있다.

제안: Python에서 `math.isfinite(float(score))`를 강제하고, TS에서도 유한 숫자 및 정확한 점수 형식(`[+-]?\d+\.\d`)을 검증하라.

4. 위치: `develop_docs/v0.8.6/todo/P1-port-retrieval.md:82`, `develop_docs/v0.8.6/todo/baseline/P1-parity-vault.txt`

문제: 실볼트 315페이지·평균 62ms 지표의 재현 근거가 부족하다.

근거: baseline은 질의별 바이트 수와 PASS, 평균/최대 시간만 기록한다. 볼트 페이지 수 산출값, 실행 명령, OS/Node/Python 버전, 반복 횟수, 대상 볼트 식별자 또는 manifest가 없다. 실볼트 자체는 요청대로 열지 않았으므로 이 수치를 독립 검증할 수 없다.

제안: 콘텐츠를 포함하지 않는 메타데이터만 남겨라. 페이지 수, 실행 환경, 명령, 반복별 시간, 파일 목록 해시를 기록하고 315페이지 주장을 재현 가능하게 만들어야 한다.

5. 위치: `.claude/skills/wiki-lint/scripts/scope-expand.py:112-119,131`, `tools/llmwiki-mcp/src/expand.ts:34-48`

문제: lexical score가 호출당 중복 계산된다.

근거:

- Python `lexical_seeds()`가 모든 노드에 대해 `lex_score()` 계산: `scope-expand.py:114-115`
- 이후 `expand()`가 다시 모든 노드의 distinct를 계산: `scope-expand.py:131`
- TS도 `expand.ts:37`과 `expand.ts:47`에서 같은 구조다.

BM25 rerank에서는 `scoreText`, 코드포인트 길이, term count도 별도로 전체 그래프를 재순회한다.

제안: `lexicalSeeds()`가 `{slug, distinct, total}` 또는 distinct 캐시를 반환하고 `expand()`가 재사용하게 하라. 현재 315페이지 62ms라면 단순 선형 추정으로 1,000페이지는 약 197ms지만, 파일 I/O와 OneDrive 지연을 포함한 실제 <1초 보장은 아니다.

6. 위치: `tools/llmwiki-mcp/src/once.ts:20-34,71-80`, Python 원본 `search.py:127-134`, `scope-expand.py:289-298`

문제: 잘못된 CLI 옵션의 의미가 Python과 다르다.

예시:

- `--top 1.5`: Python `int()`는 실패하지만 TS `parseInt()`는 `1`로 수용한다.
- 값 없는 `--root`: Python은 오류가 나지만 TS는 빈 문자열을 사용한다.
- 값 없는 옵션도 TS는 조용히 빈 값을 만든다.

제안: 옵션 값 존재 여부와 정수 전체 문자열 검증을 Python과 동일하게 구현하라. 이 경로는 P2 도구 입력 검증과도 연결된다.

7. 위치: `tools/llmwiki-mcp/src/vault.ts:144-167`, `develop_docs/v0.8.6/DESIGN.md:176-177`

문제: `walkMd`는 symlink 엔트리는 건너뛰지만, DESIGN §5가 요구하는 각 Markdown 파일의 `realpath` 하위 경계 검증은 아직 없다.

P1 체크리스트에는 P2에서 추가한다고 적혀 있으므로 즉시 P1 포팅 불일치라고 단정할 사항은 아니지만, P2 보안 완료 조건으로 명확히 추적해야 한다.

제안: P2 진입 전에 `realpath(wiki)`와 각 파일의 `realpath`를 비교하고, `path.relative()` 기반으로 외부 경로를 거부하라.

## 확인 결과

- 현재 fixture 패리티 재실행: 14×4 비교, 56개 판정 모두 PASS, golden drift 0.
- 실제 실행은 112개 프로세스다. 56은 Python/Node 쌍을 하나의 비교 셀로 센 수치다.
- `npm test`는 코드 실행 전 Vitest 임시 디렉터리 생성 권한 오류로 16개 스위트가 모두 중단됐다. 따라서 문서의 “152건 통과”는 현재 환경에서 독립 재검증하지 못했다.
- baseline 파일은 `.gitignore` 규칙에 의해 실제로 무시되고 있다.
- 현재 확인 범위에서 `walkMd`의 symlink 엔트리 스킵과 npm pack 화이트리스트 자체의 즉시 오류는 발견하지 못했다.

총평: P2 진입 불가. fixture 패리티는 통과하지만 Windows CI 증거와 외부리뷰가 미완료이고, 대응표별 단위 테스트 DoD가 과장되어 있다. 먼저 BLOCKER를 해소하고 rerank oracle의 `NaN` 허점을 고친 뒤 P2로 넘어가야 한다.